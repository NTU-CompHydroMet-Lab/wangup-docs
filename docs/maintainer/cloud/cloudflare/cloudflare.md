# Cloudflare

Cloudflare is our DNS providor for the security.
We generate the cloudflare api token for Traefik to use DNS-01 challenge.
So that Traefik can automatically write DNS records required for Let's Encrypt verification.
