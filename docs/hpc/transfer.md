# File Transfer

🚧

Move data between your laptop, the lab, and HPC.

---

## Use the Transfer Node

[Each system has a dedicated data-transfer node — use it, not the login node, for large transfers. SFTP host: see your machine's manual.]

## rsync / scp

[Examples with a `<transfer-node>` placeholder; resume interrupted transfers with `rsync -avP`.]

## From the Lab NAS

[Push data from `/home/NAS/...` to HPC and pull results back.]

## Tips

[Don't re-transfer datasets HPC already hosts. Bundle many small files before sending.]
